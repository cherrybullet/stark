#!/usr/bin/env ruby

require 'stark'
require 'pathname'

code = Pathname('test/lexer/2.lox').read
tokens = Stark::Lexer.new.tokenize(code)
tokens.map { |t| puts [t.type, t.lexeme, t.literal].inspect }
